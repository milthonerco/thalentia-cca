<div
  id="popover"
  class="hidden fixed bottom-20 right-4 w-72 bg-white text-black rounded-xl shadow-xl p-4 z-[9999]"
>
  <div class="space-y-3">

    <!-- TITULO -->
    <h3 class="font-bold text-lg">Mi cuenta</h3>

    <!-- ACADEMIAS -->
    <div>
      <p class="text-sm font-semibold mb-1">Mis academias</p>
      <ul id="academyList" class="text-sm space-y-1">
        <!-- dinámico -->
      </ul>
    </div>

    <!-- BOTONES -->
    <button
      id="manageBtn"
      class="w-full bg-black text-white py-2 rounded"
    >
      Administrar
    </button>

    <button
      id="logoutBtn"
      class="w-full border border-gray-300 py-2 rounded"
    >
      Cerrar sesión
    </button>

  </div>
</div>